# start credentials.yml.dist
# Copy this file to credentials.yml and fill in your values.
# credentials.yml is gitignored — never commit actual keys.
anthropic_api_key: ""
claude_code_oauth_token: ""
# end credentials.yml.dist
